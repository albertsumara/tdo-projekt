name: Release Docker Image

on:
  push:
    tags:
      - 'v*' # Uruchamia się dla tagów v1.0, v1.0.1 itd.

jobs:
  push_to_registry:
    name: Push Docker image to GitHub Packages
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to GitHub Docker Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          # Ścieżka do Twojego Dockerfile
          file: ./CarRentalApp/Dockerfile 
          push: true
          # Nazwy tagów wpisane małymi literami (wymóg Dockera/GHCR)
          tags: |
            ghcr.io/albertsumaral4/tdo-projekt:latest
            ghcr.io/albertsumaral4/tdo-projekt:${{ github.ref_name }}